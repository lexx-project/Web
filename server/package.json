{
  "name": "saweria-qr-wrapper",
  "version": "1.0.0",
  "description": "Wrapper untuk menjalankan server Saweria dari folder /server",
  "scripts": {
    "start": "cd server && npm install && node server.js"
  }
}
